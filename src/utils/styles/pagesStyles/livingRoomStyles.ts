import Box from "@mui/material/Box";
import { styled } from "@mui/system";
import { Button, Stack } from "@mui/material";
import { Typography } from '@mui/material';
import { colors } from "../colors";

export const CoffeeTable = styled(Box)({
    position: "absolute",
    left: 195,
    top: -30,
    width: 400,
    height: 400,
    zIndex: 15,
})

export const Laptop = styled(Box)({
    position: "absolute",
    left: 260,
    top: 30,
    width: 200,
    height: 100,
    zIndex: 15,
})

export const Plant = styled(Box)({
    position: "absolute",
    left: 485,
    top: -40,
    width: 200,
    height: 300,
    zIndex: 15,
})

export const Socket = styled(Box)({
    position: "absolute",
    left: -400,
    top: 140,
    width: 30,
    height: 30,
    zIndex: 15,
})

export const Carpet = styled(Box)({
    position: "absolute",
    left: -200,
    top: 140,
    width: 400,
    height: 400,
    zIndex: 15,
    rotate: "351deg"
})

export const Sofa = styled(Box)({
    position: "absolute",
    left: -1085,
    top: 75,
    width: 600,
    height: 400,
    zIndex: 15,
    rotate: "2deg"
})

export const TV = styled(Box)({
    position: "absolute",
    left: -440,
    top: -290,
    width: 400,
    height: 300,
    zIndex: 15,
})